import { PagePlaceholder } from '../_components/page-placeholder';

export default function GamificationPage() {
  return (
    <PagePlaceholder
      eyebrow="Motivation"
      items={['Streaks', 'Milestones', 'Rewards']}
      summary="Gamification content can render achievements and progress without a separate navigation layer."
    />
  );
}
